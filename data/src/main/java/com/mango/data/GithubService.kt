package com.mango.data

interface GithubService {

    //Example
    /*@GET("search/users")
    suspend fun getSearchUsers(
        @Query("q") q: String,
        @Query("page") page: Int,
        @Query("per_page") perPage: Int
    ): DataEntityGithub
*/

}